# SpeedyNote Docker Build Guide

This guide explains how to build the SpeedyNote application using Docker and docker-compose.

## Building with Docker

Build the Docker image:
   ```
   docker-compose up -d
   ```

These files can be accessed directly on the host machine through Docker volume mounts.
